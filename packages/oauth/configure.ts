import type Configure from '@adonisjs/core/commands/configure'

/**
 * Configure hook, run by `node ace configure @dinko/adonis-oauth`.
 */
export async function configure(_command: Configure) {
  // TODO: generate config/oauth.ts, the authorization codes migration,
  // register the provider and the OAuth routes.
}
